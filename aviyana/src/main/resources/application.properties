spring.application.name=aviyana



# ? Database Configuration (Render will inject env vars)
spring.datasource.url=${DATASOURCE_URL}
spring.datasource.username=${DATASOURCE_USER}
spring.datasource.password=${DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ? JPA / Hibernate Settings
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ? Razorpay Keys (optional - replace when needed)
razorpay.key.id=rzp_test_RQwLEWKItg04vZ
razorpay.key.secret=ji2bahn3tSuYJXu8V0oCkKHF

spring.main.allow-bean-definition-overriding=true
logging.level.org.springframework.web=INFO
